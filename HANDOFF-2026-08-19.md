# 인수인계 — 2026-08-19 시점 (스키마 트랙 → [P] 세션)

새 채팅창에서 이어가기 위한 문서. **대화 기록은 안 넘어가지만 이 파일과 git 이력·코드는
그대로 남는다.** 이 문서와 실제 코드가 어긋나면 **코드를 믿을 것.**

이전 인수인계는 `HANDOFF-2026-08-18.md`(파싱 트랙, [P] 세션 작성)다. 그 문서 §7이
"스키마 트랙과 합류하려면"으로 남겨둔 순서를 이번에 전부 실행했고, 그 결과가 이 문서다.

**계획 F(2026-08-19 회의 결과) 기준**: 1단계 3·4번, 2단계 5·6번이 이 문서가 다루는 범위다.
1단계 1·2번과 2단계 7번, 3~5단계 전부는 **[P] 세션 몫**이며 아직 손대지 않았다.

## 0. 위치

```
저장소: c:\Users\cccjj\cginside\repo-analysis\paddle-gemma-orchestrator\nh-ad-review-poc
로컬 브랜치: schema/product-group-v2 (작업은 여기서 계속 진행됨, 워킹트리에 §7 항목
            커밋 안 된 채로 남아 있음 — 건드리지 말 것)

**원격 main 에 이미 push 완료(2026-08-19)**: 9316763 → 2062a7a, 24커밋, fast-forward.
그 전까지 원격엔 8/7 이전 것만 있었고 8/7 이후 파싱 수정 20개 + 스키마 4개가 전부
로컬에만 있었다. `feat/normalized-export`(895ef0f) 위에 스키마 커밋 4개만 얹은 선형
상태였던 걸(`git merge-base --is-ancestor` 로 확인) `git push origin
schema/product-group-v2:main` 으로 로컬 워킹트리를 건드리지 않고 바로 밀었다.

새 세션에서 이어가려면:
    git pull origin main      # 또는 그냥 schema/product-group-v2 에서 계속 진행

테스트: 278개 통과 (uv run python -m pytest tests/ -q)
```

**병합은 일부러 안 했다** — [P] 세션이 지금 그 브랜치에서 작업 중일 수 있어 이 세션에서
건드리면 충돌 위험이 있다. 병합은 [P] 세션이 안전할 때 직접 하는 것을 권한다.

## 1. 이 기간에 한 일 — 스키마 v2 설계 + 실제 추출 실행 검증

### 1-1. 무엇을, 왜 바꿨나

기존 스키마는 심의 체크리스트 항목에 1:1 대응하는 구조였다. 이번에 목적을 재정의했다 —
이 계층은 심의 판정을 하지 않는다. **①OCR 정본과 VLM 후보가 갈릴 때 무슨 근거로 어느
쪽을 최종 텍스트로 삼을지 정하고(정본 확정), ②입력의 모든 텍스트를 빠짐없이 넘기고,
③광고 표현도 원문 그대로 같이 넘기는 것**이 목적이다. 상세 설계 근거는
`docs/스키마설계_상품유형_2026-08-13.md`.

| 커밋 | 요약 |
|---|---|
| `a177043` | 예금성·대출성 스키마 2종 재설계 — `_common.json`(31필드) 신설 + `대출성.json`(38필드) 신설 + `예금성.json` v2 |
| `bd4d3ab` | 완료 기준을 숫자로 못박음 + 규칙리스트 최종본(연구소 8/19 전달) 반영 |
| `ff099cf` | **실제 실행해 보고 드러난 결함 3종 수정**(§2) |
| `2062a7a` | 실행 결과 기록 |

### 1-2. 스키마 형태

```
schemas/_common.json      31필드 · 5그룹   예금성·대출성 공통(은행명·심의필·수수료 등)
schemas/예금성.json        31필드 · 4그룹   includes: _common → 합 62필드(이벤트 포함 73)
schemas/대출성.json        38필드 · 5그룹   includes: _common → 합 69필드(이벤트 포함 80)
schemas/_overlay_이벤트.json  11필드        ad_type=이벤트페이지일 때만 (이번 범위 밖, 손 안 댐)
```

필드마다 `kind`(numeric/template/verbatim/enum/collection)가 있고, 이게 정본 판정 정책을
결정한다. `verbatim`(광고 표현)은 **판정 금지·원문 그대로 보존** — 다듬으면 심의 대상이
사라진다(003 실측: '1,000명'/'1,000팀' 표기 불일치가 합쳐지며 소멸했던 전례).

체크리스트 커버리지(NH 준법감시부 97항목 기준): 예금성 36/37 · 대출성 35/36
(미커버 2건은 둘 다 '그 외 사유' 예비항목).

## 2. 실행 검증 — 예금성·대출성 둘 다 처음으로 끝단 통과

VPN 연결 후 실제 VLM 호출로 11문서(골드 5 + 프로토타입 신규 6)를 돌렸다. **직전까지
끝단(파싱→추출) 통과는 예금성 4건뿐이고 대출성은 0건이었다.** 상세는
`docs/스키마트랙_실행결과_2026-08-19.md`.

### 결과

| 지표 | 합격선 | 결과 |
|---|---|---|
| 예금성 필드 회수 | ≥42/44 | **44/44** |
| 대출성 필드 회수 | ≥6/7 | **7/7** (첫 측정) |
| `input_gap_regions` | 0 | 11/11 문서 0 |
| `absence_needs_check` | 0 | 11/11 문서 0 |
| 캐시 재생 결정론 | 산출물 동일 | **11/11 완전 동일**(elapsed_s 제외, 5문서 1.4초) |
| `errors` | 0건 | 11/11 문서 0 |

산출물: 골드 5건 `out/extracted/`, 프로토타입 신규 6건 `out_proto/extracted/`.
VLM 캐시(신규): `.vlm_cache_v2/`(135건 기록, `.gitignore` 처리됨) — 예전 `.vlm_cache/`는
v1 프롬프트 기준이라 v2와 호환 안 됨, 둘 다 유지.

**이 44/44·7/7 은 단일 관측이다.** §5(지표 규율) 그대로 "성능"으로 주장하지 말 것 —
v2를 새 baseline으로 선언하고, 이후 변경은 이 값과 비교한다.

### 실행해 보고서야 드러난 결함 3종(수정 완료, `ff099cf`)

1. **`run_extract.py:53`에도 `verify_extract.py:72`와 같은 필터가 쌍으로 있었다**
   (`pg != "예금성"`). 채점기만 고쳤으면 대출성은 추출 자체가 안 됐을 것.
2. **관측 항목의 84%가 유령**(첫 실행 49건 중 41건 빈 `quote`). strict 스키마가 배열
   *항목의 모양*은 강제해도 "항목을 만들지 마라"는 강제 못 함 — 관측할 게 없으면
   `{quote:"", evidence:[], why:""}` 한 칸을 채워 보낸다. 관측은 하류가 그대로 신뢰하는
   '의심 후보'라 유령을 넘기면 없는 위반이 그만큼 생긴다. `prune_empty_observations`
   신설(기존 `prune_empty_events`와 같은 처리). 11문서 재실행 실측: 유령 80건 제거,
   실제 관측 18건만 유지.
3. **토큰 예산을 필드 개수로만 쟀다.** 배열 필드 길이는 필드 수가 아니라 **문서
   크기**에 비례하는데, `C4_전수수집`은 필드가 3개뿐이라 상한이 2700토큰이었다.
   197영역 문서에서 응답이 중간에 끊겨 그 그룹 전체가 실패했다(하필 유실 방지
   안전망 그룹). 배열 필드에 `+1800`씩 여유를 준다(C4 는 2700→8000). 수정 후
   근거 커버리지 0.437→0.675.

## 3. 산출물 — 최종 출력이 실제로 어떻게 생겼나

`out/extracted/<문서>.json` 최상위 21키, 4층 구조:

```
① 식별      document · doc_id · product_group · ad_type · schema_id · schema_version
② 추출값    fields · observations · events · unmapped · group_analysis
③ 코드검산  review_gaps · input_gap · evidence_unbacked · unused_figures
           status_corrections · events_pruned · observations_pruned · errors
④ 집계      coverage(17지표) · elapsed_s
```

필드 한 칸: `{value, evidence, status, note}`(VLM이 냄) + `{group, evidence_score,
evidence_backed, evidence_missing}`(코드가 사후에 붙임).

004(대출성) 실측 — 금리를 구성요소별로 분리해서 검산 가능한 형태로 담는다:

```
reference_rate 3.10% + spread_rate 2.30% = 5.40%  ≠  max_rate 표기 5.45%
```

**우리는 이 불일치를 고치지 않고 그대로 넘겼다** — 이게 NH 심의사례에 실제로 기록된
지적사항('대출금리 산출기준 계산 오류')이기 때문이다. 합쳐서 담았으면 검산 자체가
불가능했다.

스키마 공백 신호 17건이 `unmapped`(kind=심의관련_필드없음)로 올라왔다 — 해시태그
(`#긴급생계비` 등), 지점·담당자·연락처, 앱 메뉴, 슬로건. 조용히 사라지지 않았다는
것이 설계 의도대로 동작한 증거다. **해시태그가 다음 필드 확장 1순위 후보.**

## 4. 아직 안 한 것 — 스키마 트랙 자체에 남은 것

| | 상태 |
|---|---|
| `reading_choice` 응답 필드 | 미구현 — 산출물에 "OCR/VLM 중 뭘 왜 골랐는지" 기록 칸이 없다 |
| `adjudicate.py`(정본 판정 우선순위) | 미구현 |
| `coverage_audit`(모든 region_id 실렸는지) | 미구현 |
| 근거대장 대출성 섹션 | 없음 → `check_coverage("대출성")`은 의도적으로 실패(모르는 상품군을 조용히 넘기면 거짓 안심을 주므로) |
| 카드상품 20건 | 범위 밖 — `product_group` 자체가 비어 있어 상품군 분류부터 안 됨 |
| 정밀도·심의 판정 정답률 | **잴 수 없음**(골드에 negative 없음, 준법팀 확인 전) |

`reading_choice`+`adjudicate.py`를 스키마 트랙에서 마저 붙일지, 아니면 지금 상태로
KL 연동을 먼저 진행할지는 **판단이 필요한 지점** — 지금 없어도 계획 F 3~5단계
(KL 내보내기·반입·방문)는 진행 가능하다.

## 5. 지표를 읽는 규율 (HANDOFF-2026-08-18 §5 와 동일, 재확인)

- 문장회수·필드회수는 **매 실행 다르다**. 단일 실행으로 개선/퇴행을 주장하지 않는다
- 결정론 층(`source ∈ {ocr, digital}`, `input_gap`, `errors`)만 단일 수치로 제시한다
- VLM 층(필드 회수·근거커버리지)은 **범위**로 제시하거나 반복 관측으로만 개선을 주장한다
- v1(1710~46869d 이전)과 v2 는 프롬프트가 전면 재작성돼 **VLM 캐시가 미스**된다.
  같은 조건 비교가 성립하지 않으므로 v1 대비 개선을 주장하지 않는다 — v2를 새
  baseline으로 선언한다

## 6. 계획 F 대비 다음 단계 (전부 [P] 세션 몫)

```
1단계 #1 [P]  prototype/ 폴더 + 케이스 9건 복사 + README        ← out/extracted, out_proto/extracted 를 옮기면 됨
1단계 #2 [P]  docs/농협KL_미확정대장.md 작성
2단계 #7 [P]  카드상품 2건 파싱만 실행 → 좌표·영역 정상 동작 확인
3단계 #8~11 [P]  kl_export.py, kl_parser/, 규격 검사, 로컬 왕복 시험
4단계 #12~14 [P]  3.11 환경 검증, 반입 패키지 목록, 미확정 대장 최종 정리
5단계 [P]  농협 방문 — 미확정 대장 한 줄씩 확정. 이게 진짜 "최종 검증"이다.
           지금 나온 44/44·7/7 은 우리 쪽 자체 채점이지 준법팀 확인이 아니다.
```

## 7. 건드리지 말 것 / 알아둘 것

- `docs/review.html`(삭제 상태) · `tools/make_review.py`(수정 상태) — 이번 스키마
  작업과 무관, [P] 세션의 별도 진행 중 작업. 이번에도 안 건드렸다.
- 모델 엔드포인트(`.env`): 모델명 `spark-gemma-4-26b-NVFP4-MTP`(`spark-` 접두어 필수,
  옛 이름이면 전 호출 400인데 러너는 exit 0이던 사고 전례 있음) — 2026-08-19 재확인,
  서버 `/models` 응답과 일치 확인함.
- 이 파일은 git 에 커밋하지 않았다(작업 메모 성격, `HANDOFF-2026-08-18.md`와 동일 관례).
