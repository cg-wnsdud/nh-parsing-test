# 진입점(Custom 파서) 전용 의존성.
#
# ⚠️ **반입은 0건이다** (2026-08-24 확인). 농협 베이스 이미지 패키지 목록
# (`2.Package.txt`, 287개)을 직접 대조했더니 아래 3개가 전부 이미 들어 있다:
#
#     fastapi           0.139.2      (우리 시험 환경 0.115.6)
#     uvicorn           0.51.0       (우리 시험 환경 0.34.0)
#     python-multipart  0.0.32       (우리 시험 환경 0.0.17)
#     gunicorn          26.0.0       ← 기동에 쓰는 것도 이미 있다
#     httpx             0.28.1  ·  starlette 1.3.1  ·  pydantic 2.13.4
#     requests 2.34.2  ·  numpy 2.4.6  ·  pillow 12.3.0
#
# 2026-08-24 이전에는 이 파일이 "아래는 반입이 필요한 것"이라 적고 있었다 — **사실과
# 다르다.** 농협 예제의 `setup-application.sh` 가 python-multipart whl 을 동봉해
# `pip install --target=` 하는 것도, 지금 이미지 기준으로는 불필요한 잔재로 보인다.
#
# **대신 남는 문제는 버전 차이다.** 우리가 왕복 시험한 버전과 농협 이미지 버전이
# 위처럼 다르다(fastapi 0.115 ↔ 0.139 은 마이너 3칸 차이). 그 조합에서 도는지는
# 농협 환경에서만 확인할 수 있다 — README §7 확인 항목으로 올렸다.
#
# 이 파일은 **로컬 왕복 시험 환경을 재현하는 용도**로 남긴다. 폐쇄망에 반입하는
# 목록이 아니다.
fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.17
