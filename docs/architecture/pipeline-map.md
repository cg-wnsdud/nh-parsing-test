# 파이프라인 구성도 — 단계별 실행 순서와 판단 주체

> 기준일 2026-08-03. 코드에서 실제 실행 경로를 따라 작성했다.
> 설계 근거·산출물 계약은 [handoff.md](../handoff.md) 참조.
>
> **2026-08-03 변경**: 의미 섹션(영역을 의미 단위로 묶는 계층)과 장식예시 격리를
> 파이프라인에서 **제거**했고, 미배정 낱줄 귀속의 VLM 호출도 없앴다(순수 좌표로 대체).
> 그에 따라 단계 번호가 ⑥~⑰ → ⑥~⑮ 로 바뀌었다. 근거는
> [vlm_judge.py](../../src/nh_parsing/vlm_judge.py) 모듈 상단 주석.

---

## 표기

| 기호 | 주체 | 무엇 |
|---|---|---|
| 🅿 | **PaddleX** | PP-StructureV3 — 텍스트 검출·인식·레이아웃 블록 |
| 🅶 | **Gemma VLM** | 의미 판단 (역할·카드·교정·분류) |
| ⬛ | **순수 코드** | 좌표 계산·규칙·픽셀 통계. 모델 호출 없음 |

**원칙**: 의미 판단은 모델이, 검산은 코드가 한다. 코드가 규칙으로 우열을 정하지 않는다.

---

## 전체 흐름

```
입력 (PDF / PNG / HWP)
  │
  ├─ 0. 라우팅 ─────────────────────────────────── ⬛
  │     PDF → 페이지별 트리아지(structured / hybrid / scan_like)
  │     HWP → 텍스트·표는 디지털 정본, 내장 이미지는 OCR 트랙
  │
  ├─ 1. 글자 획득 ──────────────────────────── ⬛ + 🅿
  │     ① 밀도 기반 타일 분할          ⬛
  │     ② 레이아웃 + OCR (타일당 1회)   🅿
  │     ③ 좌표 복원 · 중복 제거         ⬛
  │     ④ 디지털 우선 병합              ⬛
  │     ⑤ 영역 조립                     ⬛  → 여기서 남은 게 '미배정 낱줄'
  │
  ├─ 2. 구조 정리 ───────────────────────────── ⬛ + 🅶
  │     ⑥ 카드 게이트                   ⬛   스크롤·단일패널이면 호출 안 함
  │     ⑦ 카드 배정                     🅶   1회 (밀도 개수를 증거로 첨부)
  │     ⑧ 역할 판정                     🅶   페이지당 1회 (섹션 묶음 없음)
  │     ⑨ 미배정 낱줄 귀속              ⬛   좌표만 (포함 → 같은 칼럼 최근접)
  │
  ├─ 3. 통합 판독 ────────────────────────────── 🅶
  │     ⑩ 밴드 통합판독                 🅶   밴드당 1회 (교정 + 누락 회수 동시)
  │     ⑪ 통짜 스윕                     🅶   페이지당 1회 (대형 장식 타이포)
  │     ⑫ 스윕-OCR 중복 정정            🅶   중복 1건당 1회 (상한 8)
  │     ⑬ 저신뢰 재판독                 🅶   conf<0.80 라인 (상한 12)
  │     ⑭ 읽기순서 정렬 · 진단           ⬛
  │     ⑮ 광고 분류                     🅶   문서당 1회
  │
  └─ 4. STAGE_3 스키마 추출 (별도 프로세스) ─── 🅶 + ⬛
        5그룹 분할 호출                  🅶   문서당 5회
        부재 4분류 · 유령 이벤트 제거     ⬛
        bbox 재부착                      ⬛

산출물: out/json (IR) · out/llm_view (STAGE_3 입력) · out/extracted (필드)
```

---

## 단계별 상세

### 0. 라우팅 ⬛

| 입력 | 처리 |
|---|---|
| PDF `structured` | **OCR 안 돌림.** 디지털 텍스트가 정본 (003 p3) |
| PDF `hybrid` | 디지털 + OCR 병행, 겹치면 디지털 우선 |
| PDF `scan_like` | 전부 OCR. 내장 래스터 **원본 해상도**로 렌더(업스케일 금지) |
| PNG | 전부 OCR |
| HWP | 텍스트·표는 사내 파서(디지털 정본), 내장 이미지만 OCR 트랙 |

판정 기준: 텍스트 20자 미만이거나 U+FFFD 비율이 높으면 텍스트 레이어 불신.

### 1. 글자 획득 — 여기가 OCR

**① 밀도 기반 타일 분할** ⬛ — [bands.py](../../src/nh_parsing/bands.py)

세로로 긴 광고(6000px 이상)는 한 번에 못 보내므로 잘라야 한다. 예전엔 1600px마다
기계적으로 잘랐는데 **글자를 반토막 냈다.** 지금은 국소 대비(에지)로 글자 있는 행을
찾아 **글자량이 고르게 나뉘는 자리**를 고르고, 그 자리를 **글자 없는 행으로 스냅**한다.

- 밝기가 아니라 대비를 쓰는 이유: 색 배경 패널을 글자로 오인하지 않기 위해
- 높이 상한(1600px)이 필요한 이유: 상한을 빼면 성긴 구간이 2157px짜리 조각이 되고
  레이아웃 블록이 84→62로 줄어 작은 영역이 큰 영역에 흡수된다(002 실측)

**② 레이아웃 + OCR** 🅿 — 타일당 1회. 검출·인식·레이아웃 블록을 한 번에 받는다.

**⑤ 영역 조립** ⬛ — 레이아웃 블록에 라인을 좌표로 분배. 블록이 없으면 수직 간격
군집으로 폴백. **어느 블록에도 안 들어간 라인이 `unassigned_lines`** 가 되고, 이게
뒤의 귀속 단계(⑨)가 존재하는 이유다.

### 2. 구조 정리

**⑥ 카드 게이트** ⬛ — 세로 스크롤(높이/폭 ≥ 2)이거나 단일 패널이면 **호출조차 안 한다.**
결정론 라우팅으로 헛호출을 막는다.

**⑦ 카드 배정** 🅶 — 개수는 픽셀 밀도가 결정론적으로 세고, **배정은 VLM**이 한다
(전폭 헤드라인처럼 어느 컬럼에도 안 속하는 요소는 좌표로 못 정하기 때문).
밀도 관측을 **증거로 프롬프트에 실어** 1회만 묻는다 — 예전엔 밀도와 어긋나면 같은
질문을 3번 더 던졌는데, 새 정보가 없으니 같은 답만 돌아왔다.

**⑧ 역할 판정** 🅶 — 영역마다 `role`(제목/본문/유의사항/고지문구/표/이미지/각주)만
정한다. **OCR 정본만 보고 판정한다**(VLM 후보는 안 씀 — 비결정성 격리).

> **2026-08-03: 섹션(의미 묶음)을 여기서 뺐다.** 예전에는 같은 호출로 영역을 의미
> 섹션으로 묶고 `group_no`까지 부여했는데 세 가지 이유로 없앴다.
> ① **재현되지 않았다** — 같은 입력을 2회 돌려 4문서 중 3문서가 불일치.
> ② **담을 자리가 없다** — 하류 계약(NormalizedDocument v1: 문서→페이지→블록)에
> 섹션 계층이 없다. ③ **파싱이 답할 질문이 아니다** — "이 영역들이 한 의미 단위인가"는
> 의미 판단이고, 파싱은 "글자가 무엇이고 어디 있나"까지가 책임이다.
> 대신 영역을 **읽는 순서로 평면 나열**한다(카드 → 위→아래 → 좌→우) — 순서 자체가
> 화면 흐름을 담으므로 STAGE_3 가 문맥을 잃지 않는다.
>
> `role` 은 남겼다. 하류 계약의 `LayoutBlock.blockType` 에 자리가 있고, 검수 화면에서
> 유의사항 영역을 골라 보는 데 쓴다. 단 **미리보기 그림에는 역할 색을 쓰지 않는다** —
> 실행 간 97.3% 일치(225개 중 6개 불일치)라, 좌표 그림에 색으로 박으면 같은 광고인데
> 실행마다 다른 그림이 나온다([pipeline.py::_save_preview](../../src/nh_parsing/pipeline.py) 주석).

**⑨ 미배정 낱줄 귀속** ⬛ — [pipeline.py::_absorb_unassigned_into_regions](../../src/nh_parsing/pipeline.py)

⑤에서 어느 영역에도 안 들어간 낱줄을 **좌표만 보고** 영역에 붙인다(VLM 호출 없음).

1. **포함**: 라인 중심점을 품는 영역 — 여럿이면 가장 작은(구체적인) 것
2. **근접**: 품는 영역이 없으면 **같은 칼럼**(가로 구간이 라인 폭의 50% 이상 겹침)
   중에서 수직 갭이 임계 이내인 최근접 영역
3. 둘 다 실패하면 미배정 유지 — `llm_view` 의 `unassigned` 로 STAGE_3 에 그대로 전달되므로
   텍스트가 사라지지는 않는다(근거를 영역 단위로 지목할 수 없을 뿐)

> **가로 칼럼 게이트가 왜 필요한가.** 예전에는 여기에 VLM 호출(내용 기준 귀속)이
> 있었는데 실행마다 같은 낱줄이 다른 영역에 붙어(001 실측 11줄) 없앴다. 그런데 대체한
> 좌표 코드가 **세로 갭만** 봤더니, 좌우 3분할 광고(003 표지·내지1·내지2)에서 세로 갭이
> 0인 반대쪽 패널 영역이 후보가 되어 `내지2` 라벨(x≈1587)이 표지 영역(x≈104~491)에
> 붙었다 — **영역 bbox 가 패널 3개를 관통했다**(공통영역 225개 중 17개 폭 확대,
> 그중 12개가 캔버스 절반 이상 관통). 가로 게이트를 넣어 17→2, 관통 12→0.
> 남은 2건은 원본 대조 결과 진짜 흡수(같은 문단 이어붙임)였다.

### 3. 통합 판독

**⑩ 밴드 통합판독** 🅶 — [pipeline.py::_merged_band_read](../../src/nh_parsing/pipeline.py)

**OCR이 본 것과 같은 크롭**을 VLM에도 주고 "이 영역들을 고쳐라 + 목록에 없는 문구를
찾아라"를 한 번에 묻는다. 원래 두 단계였던 것을 합친 결과 호출이 122→65회로 줄었다.

- 영역은 **겹침이 가장 큰 밴드 하나**에만 배정한다. 밴드가 200px 겹치므로 "중심이
  범위에 들면"으로 고르면 24%가 두 밴드에 중복 배정된다(2026-07-29 수정)
- 영역이 경계에 걸치면 **크롭을 넓힌다**(컷을 옮기지 않는다 — 옮기면 밀도 분할이 깨짐)

붙는 결과는 **후보**(`vlm_reading`)일 뿐 정본을 안 덮는다. 관계를 경계 기준으로 판정해
(`tail_cut` / `head_drop` / `expanded` / `diverged` / `same`) 딱지를 붙인다 —
토큰 겹침 점수만으로는 **뒤가 잘린 판독이 정밀도 만점**을 받기 때문이다.

**⑫ 스윕-OCR 중복 정정** 🅶 — 스윕이 회수한 문구가 기존 라인의 다른 판독이면
(`1O.1%p` vs `① 0.1%p`) 그 자리를 고해상 재판독해 **VLM이 심판**한다. 규칙으로
우열을 정하지 않는다.

> **2026-08-03: 이 단계와 ⑬ 의 결과도 정본을 덮지 않는다.** 예전에는 재판독 결과를
> `line.text` 에 직접 써서 OCR 정본이 사라졌다(올원e 실측: 정본 칸에 `① 0.1%p : 「NH
> 올원e통장」…` 이 들어가 있었다 — 예뻐 보이지만 실행마다 바뀌는 값). 지금은
> `line.vlm_reading` 후보로만 붙는다 — 영역 단위 `Region.vlm_reading` 과 같은 원칙(B안).

**⑭ 진단** ⬛ — 레이아웃이 통째로 놓친 블록이 있으면 노트로 남긴다
([layout_gap.py](../../src/nh_parsing/layout_gap.py)). 미배정 낱줄이 27개라는 숫자만으로는
"레이아웃이 깨진 것"인지 "원래 흩어진 화면"인지 모르므로, **응집한 덩어리만** 골라낸다.

### 4. STAGE_3 스키마 추출

스키마를 5그룹(상품기본 / 금리 / 의무고지 / 위험표현 / 이벤트)으로 나눠 호출한다.
한 번에 57필드를 다 물으면 응답이 길어져 배열 선두가 비는 퇴행이 난다.

부재 4분류(미표시 / 해당없음 / 확인필요 / 판정제외)는 **스키마 메타데이터를 코드로
평가**한다 — 모델 호출을 늘리지 않는다.

---

## 비용 — 무캐시 실측 (2026-08-03, 5문서)

| 단계 | 호출 | 초 | 비중 | 호출당 |
|---|---:|---:|---:|---:|
| ⑩ `band_read` | 27 | 548.1 | **64%** | 20.3s |
| ⑪ `missing_text_sweep` | 6 | 93.0 | 11% | 15.5s |
| ⑧ `region_roles` | 6 | 90.2 | 11% | 15.0s |
| ⑫ `line_crop_reread` | 14 | 47.0 | 6% | 3.4s |
| ⑬ `lowconf_line_reread` | 10 | 32.1 | 4% | 3.2s |
| ⑮ `ad_classification` | 4 | 21.1 | 2% | 5.3s |
| ⑦ `card_assign` | 2 | 20.6 | 2% | 10.3s |
| **파싱 합계** | **69** | **852.1** | | |
| STAGE_3 | 20 | 627.4 | (별도) | 31.4s |

전체 25분 7초 = 파싱 14분 37초 + STAGE_3 10분 28초 + 채점·검증·리뷰 2초.
**파싱 14분 37초 중 852초(97%)가 모델 대기다.** OCR·좌표계산·규칙판정은 다 합쳐 3% 미만.
추론 서버가 공유 자원이라 실행마다 크게 흔들리므로(같은 코드로 `band_read` 422초↔548초 실측),
개선 효과는 **시간이 아니라 호출 수로** 판정한다.

> 없어진 단계: `orphan_sections`(⑪ 내용 귀속, 4호출/28초) — 순수 좌표 코드로 대체.
> `card_assign` 이 4→2 호출로 줄어든 것은 카드 게이트가 걸러낸 결과다(문서 구성에 따라 달라짐).

---

## 검증 도구

| 도구 | 대상 | 지표 | 현재 (2026-08-03 무캐시) |
|---|---|---|---|
| `evaluate.py` | `out/json` | 분류 / **영역검출** / 문장 | 9/9 · 43/44 · 237/242 |
| `verify_extract.py` | `out/extracted` | 필드 회수 | 43/44 |
| `make_review.py` | 전부 | 육안 검수 HTML | `out/review.html` |
| `vlm_cache.py` | — | A/B 결정론 (`=r` 기록 / `=p` 재생) | 개발 전용 |

`make_review.py`가 그리는 것: 원본 위 bbox 하이라이트, STAGE_3 필드와 근거 연결,
**OCR 정본 ↔ VLM 후보 대조**(갈린 영역만, 위험한 관계를 위로).

> ⚠️ **`evaluate.py` 의 지표 1이 '섹션 검출'에서 '영역검출'로 바뀌었다** — 옛 `37/44(84%)`
> 와 **직접 비교할 수 없다**(측정 대상이 다름). 섹션 생성을 없앴으므로 타입 일치를
> 요구할 대상이 없어졌고, 같은 골드 box 를 "그 자리에서 글자를 건졌나"로만 쓴다.
> (섹션 제거 직후 이 채점기가 사라진 구조를 계속 읽어 **크래시 없이 조용히 0/44** 를
> 내고, `llm_view` 도 옛 형태로 훑어 정상 문장까지 "STAGE_3 입력 누락"으로 오표시했다.
> 커밋 `6a65dab` 에서 고쳤다 — 채점기가 코드 변경을 못 따라가면 없던 퇴행이 보인다.)
>
> 필드 회수 `43/44` 의 미회수 1건은 001 경품 금액 `20,000원` 이고, **파싱은 잡았으나
> STAGE_3 가 필드로 못 담은 것**이다(지폐 목업 그림 속 숫자 — 주변 텍스트가
> `10000 / AA 1234567 8 / 20,000원`). 동일 코드로 돌린 실행에서 잡힐 때도 있어
> STAGE_3 실행 변동이며, `unused_figures` 에 기록돼 추적은 된다.

> ⚠️ `VLM_CACHE=p`(재생)는 **프롬프트를 바꾸는 변경의 A/B에 쓰면 안 된다.** 키가
> 달라져 전량 캐시 미스가 나는데, 재생 모드는 실호출로 넘어가지 않고 그 단계를
> 실패시킨다. 실패가 폴백에 먹혀 **품질 대폭락처럼 보인다.**

---

## 알려진 한계

| 한계 | 실측 |
|---|---|
| 역할 판정 실행 간 변동 | 225개 중 6개(97.3% 일치). **정본 텍스트·bbox 는 100% 일치** |
| 회전 텍스트 OCR 실패 | 002 `최고 연 7.1%` → 정본 `최고\n[요` (VLM이 후보로 회수, 필드엔 못 들어감) |
| 그림 속 숫자 | 001 경품 `20,000원` — 파싱은 됐으나 필드 미수용, `unused_figures` 에 기록 |
| 카드 경계 판정 | 003 표지 카드 헤드라인 1개가 실행마다 소속 흔들림. 각 카드 **내용**은 안 섞임 |
| HWP 좌표 없음 | 화면 하이라이트 불가 |
| 대출성 스키마 없음 | 004는 파싱만, 추출 스킵 |
| 시인성·로고 판정 불가 | 텍스트 추출로는 잴 수 없는 항목(보류, 제외 아님) — 근거대장 `source=layout` 3건 |

> **겹친 영역**(문단 뒷줄이 별도 영역이 되는 것) 항목은 뺐다. 예전에는 "37쌍 중 32쌍이
> 섹션 갈라짐"으로 셌는데, 섹션이 없어져 같은 방식으로 재측정할 수 없다. 영역이 겹치는
> 현상 자체는 남아 있지만, 읽는 순서 평면 나열에서는 인접 출력되므로 문맥 손실이
> 섹션 시절보다 작다 — 다시 재려면 새 지표를 정해야 한다.
